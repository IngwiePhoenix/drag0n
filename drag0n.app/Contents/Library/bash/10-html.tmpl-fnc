#!/bin/bash
# 08-html.tmpl-fnc

HTML_table() { # $1 heading, $2 cont
	echo "<table><thead><tr>"
	for thead in $1; do
		echo "<th>$thead</th>"
	done
	echo "</tr></thead><tbody>"
	for tcont in $2; do
		echo "<tr>$2</tr>"
	done
	echo "</tbody></table>"
}
